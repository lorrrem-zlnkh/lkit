#!/usr/bin/env node
// Добавляет термины в site/dictionary-data.js с алфавитной вставкой в группу буквы.
import fs from "node:fs/promises";
import path from "node:path";

const dataPath = path.resolve("site/dictionary-data.js");
const raw = await fs.readFile(dataPath, "utf8");
const data = JSON.parse(raw.replace(/^window\.DICTIONARY_DATA\s*=\s*/, "").replace(/;\s*$/, ""));

const collator = new Intl.Collator("ru");
const usedIds = new Set(data.map((e) => e.id));
const existingTerms = new Set(data.map((e) => e.term.toLowerCase().replace(/\s*\(.*/, "").trim()));

function makeId(term) {
  const clean = term.replace(/\s*\(.*/, "").trim();
  const firstWord = clean.split(/[\s-]/)[0];
  const base = firstWord.toLowerCase().replace(/[^а-яёa-z0-9]/g, "") || "term";
  if (!usedIds.has(base)) { usedIds.add(base); return base; }
  let i = 2;
  while (usedIds.has(base + i)) i++;
  usedIds.add(base + i);
  return base + i;
}

const newTerms = [
  { letter: "Б", term: "Батч (от англ. «batch» — «пакет»)", definition: "Батч — это группа однотипных задач или данных, которые обрабатывают вместе, одной пачкой, а не по одной. Например, можно отправить на обработку батч из тысячи картинок и не дёргать систему на каждую по отдельности — так быстрее и экономнее по ресурсам. В машинном обучении батчем называют порцию данных, которую модель прогоняет за один шаг обучения." },
  { letter: "В", term: "Ветка (от англ. «branch», «бранч»)", definition: "Ветка — это отдельная линия разработки в системе контроля версий, где можно менять код, не трогая основную версию. Разработчики заводят ветку под каждую фичу или фикс, спокойно в ней работают, а потом мёрджат обратно. Так несколько человек параллельно пилят разные задачи и не мешают друг другу." },
  { letter: "Д", term: "Дебаг (от англ. «debug», «debugging»)", definition: "Дебаг — это процесс поиска и устранения ошибок (багов) в коде. Разработчик идёт по программе шаг за шагом, смотрит значения переменных и ловит место, где всё идёт не так, часто с помощью специального инструмента — дебаггера. По-русски это называют отладкой." },
  { letter: "К", term: "Конфликт мержа (от англ. «merge conflict»)", definition: "Конфликт мержа — это ситуация, когда Git не может автоматически объединить изменения, потому что в одном и том же месте файла правки разошлись в двух ветках. Тогда система помечает спорные участки, и разработчик вручную решает, какой вариант оставить. Возникает при [[мердж]]е или ребейзе, когда несколько человек редактировали один код." },
  { letter: "М", term: "Мердж (от англ. «merge» — «слияние»)", definition: "Мердж — это объединение изменений из одной ветки кода в другую в системе контроля версий вроде Git. Когда разработчик закончил работу в своей ветке, он мёрджит её, например, в основную, чтобы правки попали в общий код. Если двое меняли одно и то же место, может возникнуть [[конфликт]] мержа, который придётся разруливать вручную." },
  { letter: "М", term: "Мердж-реквест (от англ. «merge request», MR)", definition: "Мердж-реквест — это запрос на вливание изменений из одной ветки в другую, который создают в GitLab, чтобы код посмотрели коллеги перед слиянием. По сути это то же самое, что [[пул]]-реквест в GitHub: внутри обсуждают правки, оставляют комментарии и проходят код-ревью. После одобрения ветку мёрджат в основную." },
  { letter: "М", term: "Миграция (от англ. «migration»)", definition: "Миграция — это управляемое изменение структуры базы данных (например, добавление таблицы или колонки), описанное в коде и применяемое по шагам. Миграции хранят историю изменений схемы, их можно накатывать и откатывать, держа базу в синхроне с приложением. Шире словом называют и перенос данных или системы на новую платформу." },
  { letter: "П", term: "Пул-реквест (от англ. «pull request», PR)", definition: "Пул-реквест — это запрос на добавление ваших изменений в основной код проекта, который оформляют на GitHub. Вы просите команду «забрать» (pull) ваши коммиты из ветки, и перед слиянием их проверяют на код-ревью. В GitLab такой же механизм называют [[мердж]]-реквестом." },
  { letter: "Р", term: "Ребейз (от англ. «rebase»)", definition: "Ребейз — это операция в Git, которая переносит ваши коммиты на свежую верхушку другой ветки, как будто вы начинали работу уже от неё. В отличие от мёрджа, ребейз переписывает историю и делает её линейной и чистой, без лишних коммитов слияния. На общих ветках им стоит пользоваться осторожно — можно запутать коллег." },
  { letter: "С", term: "Сквош (от англ. «squash» — «сплющить»)", definition: "Сквош — это объединение нескольких коммитов в один, чтобы история была чище и читабельнее. Часто сквошат мелкие промежуточные коммиты («фикс», «ещё фикс», «опечатка») перед вливанием ветки в основную. Делается через интерактивный ребейз или опцию squash при мёрдже." },
  { letter: "С", term: "Стендап (от англ. «standup meeting»)", definition: "Стендап — это короткая ежедневная встреча команды, на которой каждый коротко рассказывает, что сделал вчера, что планирует сегодня и что мешает. Название пошло от идеи проводить её стоя, чтобы никто не растягивал на полчаса. Привычный элемент гибких методологий вроде Scrum." },
  { letter: "С", term: "Стеш (от англ. «stash» — «заначка»)", definition: "Стеш — это временное откладывание незакоммиченных изменений в сторону, чтобы переключиться на другую задачу с чистой рабочей папкой. Командой git stash правки прячутся, а потом их можно вернуть обратно. Удобно, когда надо срочно переключиться на другую ветку, а коммитить полусырое не хочется." },
  { letter: "Ф", term: "Форк (от англ. «fork» — «развилка»)", definition: "Форк — это полная копия чужого репозитория в своём аккаунте, с которой можно свободно экспериментировать, не имея прав на оригинал. Чаще всего форкают опенсорс-проекты: вносят правки у себя, а потом предлагают их автору через [[пул]]-реквест. Форком также называют ответвление проекта, который пошёл развиваться своим путём." },
  { letter: "Х", term: "Хук (от англ. «hook» — «крючок»)", definition: "Хук — это точка в программе, куда можно «зацепить» свой код, чтобы он выполнялся в нужный момент. Например, git-хуки запускают скрипты до или после коммита, а во фронтенде хуки React подключают состояние и побочные эффекты к компонентам. Идея одна: вклиниться в чужой процесс в заранее предусмотренном месте." },
  { letter: "Ч", term: "Чекаут (от англ. «checkout»)", definition: "Чекаут — это переключение на другую ветку или конкретное состояние кода в Git командой git checkout. После чекаута рабочая папка показывает файлы именно той ветки или коммита, на который вы переключились. В новых версиях Git для этого также используют git switch и git restore." },
  { letter: "Ч", term: "Черри-пик (от англ. «cherry-pick» — «собирать вишенки»)", definition: "Черри-пик — это перенос одного конкретного коммита из одной ветки в другую, без слияния всей ветки целиком. Командой git cherry-pick берут только нужное изменение — например, чтобы доставить срочный фикс в релизную ветку. Название отражает суть: выбираешь ровно ту «вишенку», что нужна." },
];

let added = 0;
const skipped = [];
for (const t of newTerms) {
  const key = t.term.toLowerCase().replace(/\s*\(.*/, "").trim();
  if (existingTerms.has(key)) { skipped.push(t.term); continue; }
  const entry = { id: makeId(t.term), term: t.term, definition: t.definition, letter: t.letter };
  // индексы группы этой буквы
  const groupIdx = [];
  data.forEach((e, i) => { if (e.letter === t.letter) groupIdx.push(i); });
  let insertAt;
  if (!groupIdx.length) {
    insertAt = data.length; // буквы нет — в конец (маловероятно)
  } else {
    const target = groupIdx.find((i) => collator.compare(data[i].term, entry.term) > 0);
    insertAt = target !== undefined ? target : groupIdx[groupIdx.length - 1] + 1;
  }
  data.splice(insertAt, 0, entry);
  existingTerms.add(key);
  added++;
}

await fs.writeFile(dataPath, `window.DICTIONARY_DATA = ${JSON.stringify(data, null, 2)};\n`, "utf8");
console.log(`Добавлено: ${added}, пропущено (дубли): ${skipped.length}`);
if (skipped.length) console.log("  пропущены:", skipped.join("; "));
console.log("Всего терминов:", data.length);
